apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: greenearth-prod

resources:
  - ../../base
  - elasticsearch-pod-monitoring.yaml
  - max-map-count-daemonset.yaml
  - elasticsearch-master-pdb.yaml
  - elasticsearch-data-pdb.yaml

patches:
  - target:
      kind: Elasticsearch
      name: greenearth
    patch: |-
      - op: replace
        path: /spec/nodeSets/0/name
        value: master
      - op: replace
        path: /spec/nodeSets/0/count
        value: 3
      - op: replace
        path: /spec/nodeSets/0/config
        value:
          node.roles: ["master"]
          node.store.allow_mmap: true
      - op: add
        path: /spec/nodeSets/0/config/node.store.allow_mmap
        value: true
      - op: add
        path: /spec/nodeSets/0/podTemplate/spec/affinity
        value:
          podAntiAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
              - labelSelector:
                  matchLabels:
                    elasticsearch.k8s.elastic.co/statefulset-name: greenearth-es-master
                topologyKey: kubernetes.io/hostname
      - op: add
        path: /spec/podDisruptionBudget
        value: {}
      - op: replace
        path: /spec/nodeSets/0/podTemplate/spec/containers/0/resources/requests/memory
        value: 6Gi
      - op: replace
        path: /spec/nodeSets/0/podTemplate/spec/containers/0/resources/limits/memory
        value: 6Gi
      - op: replace
        path: /spec/nodeSets/0/podTemplate/spec/containers/0/resources/requests/cpu
        value: "200m"
      - op: replace
        path: /spec/nodeSets/0/podTemplate/spec/containers/0/resources/limits/cpu
        value: "200m"
      - op: replace
        path: /spec/nodeSets/0/podTemplate/spec/containers/0/env/0/value
        value: "-Xms3g -Xmx3g"
      - op: replace
        path: /spec/nodeSets/0/volumeClaimTemplates/0/spec/resources/requests/storage
        value: 16Gi
      - op: add
        path: /spec/nodeSets/-
        value:
          name: data
          count: 4
          config:
            node.roles: ["data", "ingest"]
            node.store.allow_mmap: true
          podTemplate:
            spec:
              serviceAccountName: es-node-sa
              containers:
              - name: elasticsearch
                resources:
                  requests:
                    memory: 48Gi
                    cpu: "7385m"
                  limits:
                    memory: 48Gi
                    cpu: "7385m"
                env:
                - name: ES_JAVA_OPTS
                  value: "-Xms8g -Xmx8g"
          volumeClaimTemplates:
          - metadata:
              name: elasticsearch-data
            spec:
              accessModes:
              - ReadWriteOnce
              resources:
                requests:
                  storage: 2048Gi
  - target:
      kind: Kibana
      name: greenearth
    patch: |-
      - op: replace
        path: /spec/podTemplate/spec/containers/0/resources/requests/memory
        value: 1Gi
      - op: replace
        path: /spec/podTemplate/spec/containers/0/resources/limits/memory
        value: 1Gi
      - op: replace
        path: /spec/podTemplate/spec/containers/0/resources/requests/cpu
        value: "500m"
      - op: replace
        path: /spec/podTemplate/spec/containers/0/resources/limits/cpu
        value: "500m"
  - target:
      kind: ServiceAccount
      name: es-node-sa
    patch: |-
      - op: replace
        path: /metadata/annotations/iam.gke.io~1gcp-service-account
        value: es-snapshot-prod@greenearth-471522.iam.gserviceaccount.com

configMapGenerator:
  - name: snapshot-settings
    behavior: replace
    literals:
      - snapshot_bucket=greenearth-471522-es-snapshots-prod
      - snapshot_schedule=0 0 9 * * ?
      - snapshot_expire_after=14d
      - snapshot_max_count=14
  - name: index-settings
    behavior: replace
    literals:
      - index_shards=60
      - index_replicas=1
      - hashtag_index_shards=10
      - hashtag_index_replicas=1
      - inference_index_shards=4
      - inference_index_replicas=1
      - inference_max_age=1d
  - name: ilm-settings
    behavior: replace
    literals:
      - posts_index_shards=3
      - posts_index_replicas=1
      # The quality index holds ~4.6% of the posts corpus (~773k docs @14d), so a
      # single primary shard is both sufficient and preferable: kNN fans out to
      # every shard, and fewer shards means fewer HNSW graphs consulted per query.
      - posts_quality_index_shards=1
      - posts_quality_index_replicas=1
      - posts_quality_ilm_delete_age=14d
      - likes_index_shards=2
      - likes_index_replicas=1
      - tombstones_index_shards=3
      - tombstones_index_replicas=1
      - replies_index_shards=3
      - replies_index_replicas=1
      - ilm_delete_age=60d
      - ilm_forcemerge_age=8d
      - index_period=week
